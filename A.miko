imu Test =
{
    imu readonly A(imu a = int, imu b = int) =
    {
        imu A = a;
        imu readonly B = b;

        C = a + b;

        aBigb = [
            if(a > b) = true,
            false
        ]

        block AChenBBigZero
        {
            tmp = a * b;

            self = if(tmp > 0) = tmp;
        }
    }

    imu readonly B =
    {
        A = A(1， 2)
        A.AChenBBigZero = null;
        open A;
    }
}

//local

//block A(must imu a = int)